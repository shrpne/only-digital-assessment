import React from 'react';
import GlobalStyle from '@/globalStyles';
import IndexPage from '@/pages/IndexPage';

export default () => {
    return (
        <>
            <GlobalStyle />
            <IndexPage />
        </>
    );
};
